/** @test {indexController} */
describe('TDD', () => {
  describe('Index Controller', () => {
    it('valid', () => {
      let isTrue = true;
      assert.isTrue(isTrue, 'verdadeiro');
    });
  });

});
